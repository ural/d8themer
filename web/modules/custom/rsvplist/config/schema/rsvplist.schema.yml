#Scemra for the configuration file of rsvplist module
rsvplist.settings:
  type: config_object
  label: 'RSVP List Content Type Settings'
  mapping:
    allowed_types:
      type: sequence
      lable: 'Content types RSVP Form can display on'
      sequence:
        type: string
        label: 'Content type'